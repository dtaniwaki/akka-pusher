machine:
  java:
    version: oraclejdk8
  timezone: UTC
  environment:
    SBT_OPTS:
dependencies:
  cache_directories:
    - "~/.sbt"
  override:
    - echo "N/A"
test:
  override:
    - cat /dev/null | sbt clean coverage test
general:
  artifacts:
    - target/*/scoverage-report
